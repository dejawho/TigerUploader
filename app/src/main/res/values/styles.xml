<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
    </style>
    <style name="ToolbarColoredArrow" parent="AppTheme">
        <item name="android:textColorSecondary">@color/colorArrow</item>
        <item name="android:background">?attr/colorPrimary</item>
        <item name="colorAccent">@color/colorArrow</item>
        <item name="android:textColorPrimary">@color/colorArrow</item>
        <item name="actionMenuTextColor">@color/colorArrow</item>
    </style>
</resources>
